
target 'Example-iOS' do
    platform :ios, '8.0'
    pod 'RFMessageManager', :path => '.', :subspecs => ['Manager', 'SVProgressHUD']
    
    target 'Test-iOS' do
    end
end

target 'Test-macOS' do
    platform :osx, '10.8'
    pod 'RFMessageManager', :path => '.', :subspecs => ['Manager', 'RFNetworkActivityIndicatorMessage']
end
